@model IEnumerable<MIS4200CentricTeam4.Models.Feedback>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.feedbackID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.feedbackName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.feedbackID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.feedbackName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.employeeName }) |
            @Html.ActionLink("Details", "Details", new { id=item.employeeName }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.employeeName })
        </td>
    </tr>
}

</table>
